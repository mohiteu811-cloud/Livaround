{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -n \"$GITHUB_PAT\" ]; then git clone https://${GITHUB_PAT}@github.com/mohiteu811-cloud/livaround-commercial.git ../commercial 2>/dev/null || true; fi && npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "sh start.sh",
    "healthcheckPath": "/health"
  }
}
